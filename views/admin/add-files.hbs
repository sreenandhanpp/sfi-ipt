<!-- admin/add-files.hbs -->

<section>
  <div class="container mt-5">
    <div class="row">
      <div class="col-md-6">
        <h2 class="text-center">Add Notes</h2>

        <form
          action="/admin/add-files"
          method="post"
          enctype="multipart/form-data"
        >
 <div class="form-group input-container">
                <label for="year">Select Department</label>
                <select class="form-control input " name="department" id="department">
                  <option 
                  value="c">{{department}} </option>
                </select>
              </div>
          <div class="form-group input-container">
            <label for="year">Select Academic Year</label>
            <select class="form-control input" name="year" id="yearDropdown">
              <option value="">Select</option>
              <option value="1">1st Year</option>
              <option value="2">2nd Year</option>
              <option value="3">3rd Year</option>
            </select>
          </div>
          <div class="form-group input-container">
            <label for="semester">Select Semester</label>
            <select class="form-control input" name="sem" id="semesterDropdown">
              <option value="">Select</option>
              <option class="year1" value="1">Semester 1</option>
              <option class="year1" value="2">Semester 2</option>
              <option class="year2" value="3">Semester 3</option>
              <option class="year2" value="4">Semester 4</option>
              <option class="year3" value="5">Semester 5</option>
              <option class="year3" value="6">Semester 6</option>
            </select>
          </div>

          <div class="form-group">
            <label for="subject">Select Subject</label>
            <select class="form-control" name="name" id="subjectDropdown">
              <!-- Empty dropdown, to be populated dynamically -->
            </select>
          </div>

          <div class="form-group">
            <label for="text">Content</label>
            <input type="text" class="form-control" name="content" />
          </div>

          <div class="form-group">
            <label for="file">PDF File</label>
            <input type="file" class="form-control" name="file" accept=".pdf" />
          </div>

          <button type="submit" class="btn btn-success mt-4">Submit</button>
        </form>
      </div>
    </div>
  </div>
</section>

<script>
  
function updateSemesterDropdown() {
    var selectedYear = document.getElementById("yearDropdown").value;
    console.log("Selected Year:", selectedYear);

    var semesterOptions = document.querySelectorAll("#semesterDropdown option");
    console.log("All Semester Options:", semesterOptions);

    // Hide all semester options
    semesterOptions.forEach(function (option) {
        option.style.display = "none";
    });

    // Show semester options corresponding to the selected year
    var yearSpecificOptions =
         document.querySelectorAll("#semesterDropdown option.year" + selectedYear);

    yearSpecificOptions.forEach(function (option) {
        option.style.display = "block";
    });

    // Reset the selected option
    document.getElementById("semesterDropdown").value = '';

    // Load subjects after updating semester dropdown
    loadSubjects();
}

// Initial update based on the default selected year
updateSemesterDropdown();

// Bind the update function to the change event of the year dropdown
document.getElementById("yearDropdown").addEventListener("change", updateSemesterDropdown);


function loadSubjects() {
    const selectedYear = document.getElementById('yearDropdown').value;
    const selectedDepartment = document.getElementById('department').value;
    const selectedSemester = document.getElementById('semesterDropdown').value;

    fetch('/admin/filtered-subjects', {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
        },
        body: JSON.stringify({ department: selectedDepartment, year: selectedYear, semester: selectedSemester }),
    })
        .then(response => response.json())
        .then(data => {
            const subjectDropdown = document.getElementById('subjectDropdown');
            subjectDropdown.innerHTML = '';

            data.subjects.forEach(subject => {
                const option = document.createElement('option');
                option.value = subject;
                option.text = subject;
                subjectDropdown.appendChild(option);
            });
        })
        .catch(error => console.error('Error fetching subjects:', error));
}

document.getElementById('semesterDropdown').addEventListener('change', loadSubjects);
</script>